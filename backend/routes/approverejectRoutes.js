// routes/travelRequestRoutes.jsconst express = require('express');const router = express.Router();const travelRequestController = require('../controllers/travelRequestController');const authMiddleware = require('../middlewares/auth'); // Authentication middleware// Approve a travel requestrouter.put('/travelrequests/:id/approve', authMiddleware, travelRequestController.approveRequest);// Reject a travel requestrouter.put('/travelrequests/:id/reject', authMiddleware, travelRequestController.rejectRequest);module.exports = router;